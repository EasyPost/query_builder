source "https://rubygems.org"

gem 'attributes_dsl', git: 'https://github.com/easypost/attributes_dsl.git', tag: '0.2.2'

gemspec

group :metrics do
  gem "hexx-suit", "~> 2.3" if RUBY_ENGINE == "ruby" && RUBY_VERSION >= "2.1"
end
